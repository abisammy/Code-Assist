Lets say you want to have an embed, here is an example
```js
const { channel } = message
const { MessageEmbed } = require("discord.js")

channel.send(embed)

const embed = new MessageEmbed()
.setAuthor("this is an embed")
.setDescription("Isnt it cool")
.setColor("#FF0000")
.addField('But it isnt gonna work", "😢😢😢");
```

What is the problem with that code you may ask, well I will show you
```js
channel.send(embed)
//           ^^^^^
//           You send the embed, bewfor defining it
const embed = new MessageEmbed()
.setAuthor("this is an embed")
.setDescription("Isnt it cool")
.setColor("#FF0000")
.addField("But it isnt gonna work", "😢😢😢");
// ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
// You need to put these lines of code above the line when you send it
```

If you are stil having problems at this point ask someone to help you
*Written by abisammy#4749*